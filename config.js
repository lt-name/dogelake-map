// API后端配置
const CONFIG = {
    // 主地图后端地址（用于map.html）
    mainMapBackend: 'http://127.0.0.1:8080',

    // 纯净版地图后端地址（用于map-only.html）
    mapOnlyBackend: 'http://127.0.0.1:8080',

    // LLSE后端路径（如果后端API在根路径下，保持为空字符串）
    llsePath: ''
};
